// string methods 

const fullNaMe = "ankit is king don no 1";
const fullNaMe2 = "ankit is king don no 1";

const fullNameArr = fullNaMe.split(" ");

// console.log(fullNameArr) (it will give you new array removing space)

// isName = fullNaMe.includes("aman") (if text is available in line it will return true)

if(isName){
    console.log(`Yes is indludes in this line`)
}
else{
    console.log(`no, "is" not includes in this line`)
    
}
